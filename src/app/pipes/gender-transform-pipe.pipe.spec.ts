import { GenderTransformPipePipe } from './gender-transform-pipe.pipe';

describe('GenderTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GenderTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
