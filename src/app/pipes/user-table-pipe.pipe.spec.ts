import { UserTablePipePipe } from './user-table-pipe.pipe';

describe('UserTablePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UserTablePipePipe();
    expect(pipe).toBeTruthy();
  });
});
